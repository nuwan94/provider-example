class Product {
  int id;
  String name;
  double price;
  int quantity;
  String image;
  String description;

  Product(this.id, this.name, this.price, this.quantity, this.image,
      this.description);
}
